题目4：基于Qwen2.5-1.5B实现NatureLM Stage A 单卡训练Pipeline
NatureLM（https://arxiv.org/pdf/2502/07527）是一个面向科学发现的序列基础模型，其核心思想是将小分子/蛋白质/DNA/RNA/材料学和自然语言统一表示为token序列，并在大语言模型架构上进行科学数据继续预训练。论文中指出，NatureLM的预训练语料包含文本/小分子/蛋白质/材料/DNA/RNA以及跨领域数据，总规模约为143.8B tokens；其中蛋白/RNA/DNA/小分子和跨领域数据占据主要比例。
在NatureLM中，不同学科对象被转换为统一的文本序列格式。例如，小分子用SMILES表示，蛋白在/RNA/DNA使用FASTA风格序列表示，材料则将化学组成和空间群信息展平成序列。为了区分不同科学实体，论文使用成对的特殊token包裹科学序列，例如mol .../mol、protein .../protein、dna .../dna、rna .../rna、material .../material等。
NatureLM模型主体建立在已有LLM之上。论文中使用Llama 3 8B和Mixtral 8x7B初始化主干模型，并为科学实体引入新的special tokens。由于这些新增科学token的embedding是随机初始化的，并可能破环原始语言模型已有的语言能力。因此，论文采用了两阶段继续预训练策略：第一阶段只训练新增token相关参数，冻结已有模型参数；第二阶段再整合训练整个网络。
本题要求你实现StageA训练pipelie。在本题中，StageA指NatureLM continued pre-training的第一阶段：扩展tokenizer与embedding后，冻结原始LLM主体参数，只训练新增科学token的embedding/lm head相关参数，使新增科学token逐步适配原有语言模型的表示空间。为简化本题实现，本题仅覆盖小分子/蛋白质/DNA/RNA/自然语言五类数据，不涉及材料数据处理。

【任务目标】
你需要在单张GPU上完成一个可运行的Stage A fine-tuning pipeline，你的代码需要做到：
1.读取 raw JSONL数据
2.根据样本的type字段，将raw sequence转换为对应模态的模型输入文本；
3.使用Qwen2.5-1.5B原始tokenizer对输入文本进行分词和编码；
4.扩展tokenizer，加入NatureLM风格科学tokens；
5.Resize Qwen2.5-1.5B的token embedding/lm head，使其与扩展后的tokenizer词表大小一致；
6.冻结原始模型参数；
7.只训练新增scientific special tokens对应的embedding/lm head参数；
8.保存tokenizer/模型checkpoint和训练log
9.输出标准化JSONL log，方便自动批改。

【数据描述】
我们提供raw data文件：data/train.jsonl，每一行是一个JSON object，格式如下：
{"type":"mol","sequence":"CCOC(=O)c1ccccc1"}
{"type":"protein","sequence":"MKTFFVLLLCTFTVV"}
{"type":"dna","sequence":"ATGCGTACCGTA"}
{"type":"rna","sequence":"AUGCGUACCGUA"}
{"type":"text","sequence":"Proteins fold into three-dimensional structures."}
你需要自己万册所有preprocessing。此外，我们提供所有科学序列词表文件data/science_tokens.txt，其中包含小分子/蛋白质/DNA/RNA四类科学模态的token。你需要读取该文件，并将其中的tokens加入Qwen2.5-1.5B tokenizer。

【数据预处理要求】
你需要根据type字段将raw sequence转换为模型输入文本。
/*以下是表格*/
tpye, 输入sequence， 转换后文本
mol, CCOC(=O)c1ccccc1, <mol><m>C<m>C<m>O<m>C<m>(<m>=<m>O<m>)<m>c<m>1<m>c<m>c<m>c<m>c<m>c<m>1</mol>
protein, MKTFFVLLLCTFTVV, <protein><a>M<a>K<a>T<a>F<a>F<a>V<a>L<a>L<a>L<a>C<a>T<a>F<a>T<a>V<a>V</protein>
dna, ATGCGTACCGTA, <dna><d>A<d>T<d>G<d>C<d>G<d>T<d>A<d>C<d>C<d>G<d>T<d>A</dna>
rna, AUGCGUACCGUA, <rna><r>A<r>U<r>G<r>C<r>G<r>U<r>A<r>C<r>C<r>G<r>U<r>A</rna>
text, 原始文本, 原始文本不加spetial token

必须加入的wrapper special tokens:
SPECIAL_TOKENS = [
    "<mol>","</mol>",
    "<protein>","</protein>",
    "<dna>","</dna>",
    "<rna>","<rna>",
]
此外，读取data/science_tokens.txt，并将其中所有科学模态tokens作为新增tokens加tokenier。

【模型权重描述】
预训练模型权重/配置文件以及原始tokenizer相关文件均存放与Qwen2.5-1.5B/目录下。

【参考评价标准】
满足以下条件即为通过：
1.代码能在单张GPU上运行
2.能够从raw JSONL数据完成预处理并启动训练；
3.使用Qwen2.5-1.5B，不使用其他base model;
4.tokenizer成功加入8个wrapper special tokens，并成功加入data/science_tokens.txt中所有的scientific vocabulary tokens；
5.新增曾tokens能被tokenizer识别为单个完整token；
6.模型的token embedding/lm head已根据扩展后的tokenizer词表大小正确resize；
7.训练过错中loss正常输出
8.原始模型bacbone冻结
9.仅在新增token embedding和lm head上存在梯度；
10.最终checkpoint和tokenizer可以重新加载
11.train_log.jsonl格式正确，可用于手动批改。

【提交内容】
本题需要提交以下内容到/vepfs/rproblem4/目录下：
/vepfs/problem4/
    train_stage_a.py
    README.md
    outputs/
        stage_a/
            train_log.jsonl
            tokenizer/
            checkpoint-50/
            final/
    data/
        train.jsonl
其中必须包含：
1.train_stage_a.py：主训练脚本，能够从raw JSONL数据开始完成数据预处理/tokenizer扩展/模型加载/embedding resize/参数冻结/StageA训练/checkpoint保存和log输出。脚本应提供合理的默认参数，在不额外传入命令行参数的情况下可直接运行并完成训练。
2.README.md：说明如何运行嗲名，包括使用的Qwen2.5-1.5B checkpoint路径/训练数据路径/运行命令/主要超参数/参数冻结方式/gradient check方法，以及如何重新加载最终模型。
3.output/stage_a/train_log.jsonl：标准化JSONL训练日志，必须包含init/data_example/train_step/grad_check/save/final_save/done等事件，供自动批改使用。至少包括如下示例：
{"event":"init", "old_vocab_size":"<int>", "num_added_tokens":"<int>", "new_vocab_size":"<int>", "train_num_examples":"<int>"}
{"event":"data_example", "type":"protein", "raw_sequence":"ACE", "processed_text":"<protein><a>A<a>C<a>E</protein>", "input_ids":["<int>","...","<int>"]}
{"event":"train_step", "step":"<int>", "loss":"<float>", "learning_rate":"<float>"}
{"event":"grad_check", "old_embedding_grad_norm":"<float>", "new_embedding_grad_norm":"<float>", "old_lm_head_grad_norm":"<float>", "new_lm_head_grad_norm":"<float>"}
{"event":"save", "step":"<int>", "path":"outputs/stage_a/checkpoint-<step>"}
{"event":"final_save", "path":"outputs/stage_a/final"}
{"event":"done", "status":"success"}
4.outputs/stage_a/final/：最终保存的模型和tokenizer，必须能够通过以下方式重新加载：
Python
from transformers import AutoTokenizer, AutoModelForCausalLM

tokenizer = AutoTokenizer.from_pretrained("/vepfs/problem4/outputs/stage_a/final")
model = AutoModelForCausalLM.from_pretrained("/vepfs/problem4/outputs/stage_a/final")

5.outputs/stage_a/tokenizer/:单独保存的扩展后tokenizer，需包含新增scientific special tokens。
